# Workshop dependencies (minimal)
# LLM API client (Vertex AI OpenAI-compat) + structured outputs
openai>=1.12.0
instructor>=1.2.0
pydantic>=2.5.0

# Auth — OAuth do Vertex AI (w Colab preinstalowany; tu dla pewności)
google-auth>=2.28.0

# Dane i wykresy
pandas>=2.1.0
matplotlib>=3.8.0

# Progress
tqdm>=4.66.0

# Interaktywne widgety (przełącznik wykresu accuracy; wymagane też przez tqdm.notebook)
ipywidgets>=8.0.0
